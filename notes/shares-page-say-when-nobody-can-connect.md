# Shares page: say when nobody can connect

- 2026-09-15 05:21 — mk-drive: Shares page warns 'Nobody can connect over SMB yet' when a share has SMB on and no user has a password, links to Settings → Account; the section is now 'Who can connect over SMB' and says box logins are not SMB accounts; the share dialog loads users and shows the warning or 'Who can connect: …'. Client builds; not yet seen on a running box (mk-drive c16705f) — main @ 605809a board: HTTPS on the local network
