# *Tell me when something is wrong** the box knows about a degraded pool, a failing or overheating disk, a failed scrub, a failed settings backup and a waiting update, but nothing reaches the owner unless they open the page: a small alert engine in the agent (one event per condition, remembered so it does not repeat, cleared when the condition goes) and delivery the drive can use — phone push through mk-drive first, email as an option; quiet by default, no chatter

- 2026-09-16 15:49 — Pairs with the mk-drive card 'Alerts from the box, on the phone': the agent owns the conditions and their state, the drive owns push and the settings UI.
- 2026-09-16 16:06 — Agent side done (37619e0): alerts.ts with pure conditions + reconcile, an alerts table that keeps since/acked across restarts, 5-minute timer plus an immediate check on ZFS events that matter, verbs alerts + alert.ack, mk-nas alerts in the CLI, docs/alerts.md. Verified in the VM: filled the pool to 92% -> raised, confirmed after a minute, acked, freed space -> cleared into history. Left for the drive card: push delivery and the UI.
- 2026-09-16 16:06 — The agent raises, confirms, acknowledges and clears conditions and keeps a month of history; delivery is the mk-drive card — main @ 37619e0 Alerts: the box says what is wrong, and when it is over
