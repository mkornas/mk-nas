# Go public

- 2026-09-15 07:24 — 2026-09-15: done — files cleaned in both repos (AGPL-3.0-only, generic docs/notes/board, neutral test names; VM login nasadmin since Ubuntu reserves admin); full history pushed to private mkornas/mk-nas-archive and mk-drive-archive (local branch pre-public too); main squashed to one commit by hi@mateuszkornas.com and force-pushed; old releases/tags and 106 Actions runs deleted; both repos public (GitHub detects AGPL-3.0); mk-drive 0.3.0 and mk-nas 0.5.0 released from the public repos. Left: old GHCR image versions (their server/test has the old subnet) need deleting — needs gh auth refresh -s read:packages,delete:packages — then the mk-drive package made public in the GitHub UI. Commit hashes in older notes resolve only in the archive repos.
- 2026-09-15 09:59 — Public: both repos (one squashed commit + release), AGPL-3.0, archives private; GHCR mk-drive package public with only 0.3.0 and latest left (207 older versions deleted, anonymous pull of 0.3.0 verified, 0.2.2 gone). Remaining outside the repos: the mk-kit 'built with' link and the blog post point at the public URLs. — main @ 6723370 board: going public — what is done, what is left
