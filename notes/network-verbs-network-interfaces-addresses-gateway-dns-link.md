# *Network verbs** network (interfaces, addresses, gateway, DNS, link speed, DHCP or static from netplan) and network.set (hostname via hostnamectl, one netplan file /etc/netplan/90-mk-nas.yaml, applied with a 120 s revert unless the UI confirms it can still reach the box — the safe way to change an address remotely); mDNS via avahi so <hostname>.local works out of the box

- 2026-09-13 08:23 — Decision: our own revert, not netplan try (its stdin/tty behaviour without a terminal is uncertain). network.set writes /etc/netplan/90-mk-nas.yaml (0600; a fixed shape rendered and parsed by regex, one entry per interface ever set, other interfaces stay with the installer's file), keeps the previous content + expiry in /var/lib/mk-nas/netplan-pending.json, runs netplan generate then apply; a refused file is put back at once with netplan's stderr. revertIfExpired runs in the network verbs, at agent start and in the tick. Hostname: hostnamectl + the 127.0.1.1 line in /etc/hosts + systemctl try-restart avahi-daemon; avahi-daemon added to Depends. UI: Storage → Network tab; Apply confirms, then a banner with the new URL (and <hostname>.local) and a countdown; Keep calls network.confirm. Not run on a real box: verify in the VM that netplan apply over ssh keeps the session on a same-address change and that the revert lands. mk-nas 6a5f750.
- 2026-09-13 08:23 — Network verbs shipped: network.ts (read, set with revert, confirm) + CLI + avahi in Depends in mk-nas 6a5f750; Storage → Network page with the keep-or-revert banner in mk-drive (NAS mode: the Network page); 68 agent and 91 drive tests pass. Needs a VM run before it is trusted on the real box. — main @ 6a5f750 Network verbs: the box's name and address, with a revert
- 2026-09-13 10:08 — VM run 2026-09-13 (mk-nasd 0.3.0 upgraded in place over apt): hostname change → hostname, /etc/hosts and <name>.local shown; network set enp0s2 --dhcp --revert 20 applied (address gone for a moment while DHCP re-ran, back within seconds), the file reverted and vanished after the timeout; the keep path leaves 90-mk-nas.yaml. Read verbs, events on the real zpool events format, scrub→job done, dataset destroy with the snapshot refusal and the location dir removed, scrub policy, tick — all fine.
