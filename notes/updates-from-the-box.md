# Updates from the box

- 2026-09-15 10:48 — Shipped in mk-nas 0.6.0 (pins mk-drive 0.4.0), the first signed release. Box: daily check in the tick, update/update.check/update.install, a runner in its own unit that verifies the ssh signature over SHA256SUMS (release-signers shipped in the package), the checksums and the drive image against release.json, backs up, apt-get installs, waits for the new agent; drive System section shows the release, notes, Install, progress and failures; mk-nas update over ssh. Release: make release signs after the workflow (install/sign-release.sh, key in ~/.config/mk-nas/release-signing-key), CI writes driveImageSha256, upgrade.sh verifies too. Verified: anonymous download of v0.6.0 verifies; VM upgraded with upgrade.sh (signature checked), then from a 0.5.9 build installed 0.6.0 from the page end to end (download, signature, package, new agent, page followed); a fake 0.9.9 failed cleanly with GitHub's 404. Follow-up commit c091e78 (failed installs clean their downloads) goes out with the next release. — main @ c091e78 Updates: a failed install leaves no downloads behind
