# Cloudflare Tunnel from the UI

- 2026-09-14 16:42 — 0.4.3: the container part is done — cloudflared 2026.9.1 is a profile-gated service in the drive's stack, started by stack-up.sh when /opt/mk-drive/.env has CLOUDFLARE_TUNNEL_TOKEN (tested in the VM: no token → no container, token → mk-drive-tunnel runs, cleared → removed; .env now 0600). What this card still needs: the agent verb that writes that line and restarts mk-drive, the Network page card (masked token, connected / last connected from the container's state or cloudflared's metrics), and refusing changes that arrive through the tunnel.
- 2026-09-15 13:36 — Agent verbs tunnel / tunnel.set / tunnel.remove (token validated by decoding, written as one .env line root-only, stack-up.sh starts or stops only the tunnel container, status from docker state + cloudflared /ready + its log, token redacted in the audit); cloudflared metrics bound to 127.0.0.1 (they were open on every address). mk-drive: 'Reach the drive from outside' card on Storage → Network, changes refused through the tunnel itself. Verified in the VM with a well-formed fake token: starting → not connected with Cloudflare's 'Failed to get tunnel', drive not restarted, .env 600, token 0× in the audit, metrics on 127.0.0.1, Turn off from the page removed the container and the line. Not yet seen with a real token: that is the first use on the pilot box. mk-nas bfae375, mk-drive 3546875. — main @ bfae375 Tunnel from the agent: tunnel, tunnel.set, tunnel.remove
