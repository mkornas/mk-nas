# Replication target mountpoint

- 2026-09-13 10:10 — receive -x mountpoint -o readonly=on (mk-nas HEAD); targets received before keep their property — zfs inherit mountpoint <target> once on the box — main @ 477ecb5 replication: the copy inherits its mountpoint and is read-only
