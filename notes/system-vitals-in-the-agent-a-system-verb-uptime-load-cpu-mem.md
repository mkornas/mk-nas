# *System vitals in the agent** a system verb: uptime, load, CPU %, memory, swap, per-NIC rates, disk I/O per pool member, hwmon temperatures, sampled every 5 s with a 30-minute ring buffer in memory (nothing on disk); mk-dashboard stays the deep monitor, this is the glance

- 2026-09-13 07:34 — Read straight from /proc and /sys (no command run), two reads make a sample; the ring keeps only totals per sample (SamplePoint) so half an hour is a few numbers a sample. NICs: everything but lo, veth*, docker*, br-*, virbr, tap, tun. Disks: whole disks by kernel name (sd*, nvme*n*, vd*, mmcblk*); the verb maps them to by-id and pool with lsblk alone — no smartctl on a 5 s path, it wakes disks. The overview shows tiles, no sparklines yet (history is there for them). mk-nas 7559961.
- 2026-09-13 07:34 — Vitals shipped: system.ts sampler + system verb + CLI in mk-nas 7559961; /api/nas/system and the tile row on the Storage overview in mk-drive (NAS mode: the box at a glance); 64 agent and 90 drive tests pass — main @ 7559961 System vitals: the box at a glance, sampled every 5 s, half an hour in memory
