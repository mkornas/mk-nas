# Phase 4 — copy

- 2026-09-12 21:14 — Agent: replication.set/remove/run/test/key, jobs with progress in SQLite, the runner as its own process (spawned by the verb, run by mk-nas-replication.timer), resumable and never -F; CLI replication …. Verified in the VM: key made, installed for root@localhost, test OK, full send created tank/backup@repl-…, job done in the jobs table and on the Copies page. mk-drive Copies page + dialog pushed (0d0f542). — main @ bd5ee8f board: system vitals and network verbs
