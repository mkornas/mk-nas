# Phase 1b — the stick

- 2026-09-12 18:22 — 2026-09-12: install/ written — install.sh (idempotent, stock 24.04, Node 24 tarball in /opt/node, agent + mk-drive compose stack as units), iso.sh (newest 24.04 live-server, checksum, xorriso replay, grub → autoinstall ds=nocloud), vm.sh (QEMU/OVMF, disks with by-id serials), user-data (interactive identity + storage) and user-data.vm (hands-free, for make vm). Admin email moved out of the stick: mk-drive's setup page creates the admin on first visit. Testing in QEMU next.
- 2026-09-12 18:50 — make vm passed on the dev box: unattended install 3 min, poweroff, boot from the OS disk, mk-drive answering on :8810 after 33 s; over the serial console: mk-nasd, mk-drive and docker active, /run/mk-nas.sock root:mk-nas 0660, the container in gid mk-nas, and the version verb answered from inside the container and audited. Gap: ghcr latest of mk-drive predates NAS mode until mk-drive main is pushed (CI builds the image). — main @ 15c5410 Phase 1b: the stick — make iso, install.sh, and the same install into a QEMU VM
