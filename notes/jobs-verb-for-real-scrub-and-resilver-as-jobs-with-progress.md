# *jobs verb for real** scrub and resilver as jobs with progress and history in SQLite; the UI polls jobs instead of parsing scan lines

- 2026-09-13 07:25 — Decision: ZFS stays the truth for the live scan — a scan job is only 'started at' plus, once the pool's scan line says it is over, how it went (done; failed when errors were found, it was canceled, or nothing is running any more). reconcileScans runs inside the jobs verb and in the tick, so history closes even when nobody opens the UI. jobs table gained a pool column (ALTER TABLE guarded by PRAGMA table_info). failDeadJobs now only sweeps replication jobs — scans have no pid and would have been marked interrupted on every runner start. Resilvers are noted only when disk.replace started them; one ZFS starts on its own shows on the scan line but not in history. mk-nas 1c7b374.
- 2026-09-13 07:25 — Scan jobs shipped: scans.ts + jobs pool filter + CLI jobs [pool] in mk-nas 1c7b374; Pools page follows the running job and lists the last five in mk-drive (NAS mode: scrubs and resilvers as jobs); 59 agent tests, 88 drive tests pass; the real-pool scrub-to-done check runs in CI — main @ 1c7b374 jobs for real: scrubs and resilvers written down as jobs, with progress and history
