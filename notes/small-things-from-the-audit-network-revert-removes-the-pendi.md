# *Small things from the audit** network revert() removes the pending file before netplan apply (a failed revert is forgotten); apply() in shares.ts writes through one fixed temp name (two share changes at once race); the socket's 64 KB limit counts the whole buffer, not a line; update downloads have no size cap; finish reading cli.ts, zfs.ts, alerts.ts, events.ts, system.ts

- 2026-09-17 18:57 — Seven fixes with tests on fix/audit-small-things (c175e47..1c4f12f), CI green incl. the root pool tests; not merged or released yet. The four listed: a network revert netplan refuses keeps its promise and is tried again (and no longer hides the Network page), share files are written one change at a time, the socket's 64 KB limit is per line, update downloads stop at a size per kind (1 MB / 512 MB package / 2 GB image). From reading the rest (cli.ts, zfs.ts, alerts.ts, events.ts, system.ts, scans.ts): a scan job's pool name from the database is checked before zpool, 'update is out' only when newer than what runs, the event log starts over when the kernel's event ids do. cli.ts and system.ts: nothing found. — fix/audit-small-things @ 1c4f12f The event log follows the kernel when its ids start over
- 2026-09-18 10:53 — 2026-09-17/18: merged and released as mk-nas 0.9.2 (docs/upgrading.md has the list), installed on the pilot box and verified: agent 0.9.2 with 0 restarts, fixes on disk, timers green. Seen then: the drive restarted with the agent, which became the agent-only update card.
