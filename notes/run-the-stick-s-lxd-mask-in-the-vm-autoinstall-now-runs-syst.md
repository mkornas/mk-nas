# *Run the stick's lxd mask in the VM** autoinstall now runs systemctl mask lxd-installer.socket in the target; it parsed but never ran: make vm, then check the socket is masked and the first user cannot trigger an LXD install

- 2026-09-17 18:47 — Fresh unattended install of 0.9.1 into a new VM (the existing VM set aside and put back): lxd-installer.socket is masked (-> /dev/null), no socket in /run, and lxc as the first user (still in the lxd group) answers 'Unable to trigger the installation of the LXD snap'; no lxd snap. rpcbind.socket and .service disabled and inactive, nothing on 111, first user not in docker. The first NFS share brought rpcbind and nfs-server up by itself and exported the dataset; after share.remove nfs-server stops, rpcbind stays up until the next boot (still disabled). share.set refused tank/global on the real agent. — main @ d5756d8 board: security audit and 0.9.1 done; two follow-ups
