# An agent-only update restarts the drive too

- 2026-09-17 20:58 — 1554410 on fix/agent-only-update, CI green; not merged or released. Wants= alone would not have worked: the container bind-mounts the one socket file and the agent made a new file on every start (inode 1508 -> 1728 on 0.9.1). So the socket is systemd's now (mk-nasd.socket, handed to the agent as fd 3); mk-drive.service requires the socket and only wants the agent; the restore finisher stops the socket unit too. VM, fresh installs with the kept VM set aside: on 0.9.1 restarting the agent restarted the drive; after the upgrade (one drive restart, expected) the drive and tunnel stayed up through restart, stop (the next call started the agent again and was answered in 151 ms), a reinstall of the package, a hard kill, with the inode unchanged; a reboot came up in order with a real socket; backup.run + backup.restore finished ok; a fresh install from a new ISO had the socket enabled and active on first boot. Behaviour change documented: systemctl stop mk-nasd alone no longer keeps the agent down. — fix/agent-only-update @ 1554410 The agent's socket is systemd's, so an agent-only update leaves the drive running
- 2026-09-18 10:53 — 2026-09-18: merged, released as mk-nas 0.9.3 (same drive 0.9.1, signed, CI package byte-identical), installed on the pilot box from its System page: the runner did the transition (old agent stopped, socket listening, drive restarted once, new agent 'from systemd'). Then Mako restarted the agent by hand: only mk-nasd stopped and started in the journal, the socket inode unchanged, the drive's uptime running since the install. Every mk-nas update that keeps the drive pin now leaves the drive and the tunnel up. Not proven from outside: the drive talking to the new agent (needs a drive login); shown in the VM with a call from inside the container.
