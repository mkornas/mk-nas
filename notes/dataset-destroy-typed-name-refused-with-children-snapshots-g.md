# *dataset.destroy** typed name; refused with children; snapshots go with it only when the caller says so; a location dataset also leaves the drive's locations

- 2026-09-13 07:13 — Agent: dataset.destroy in write.ts — typed name, refused for a pool root, children, a share or a replication; snapshots only with snapshots:true, destroyed as one comma list then the dataset (never -r); the policy row goes with it; a location's empty mount dir is rmdir'd (zfs leaves a hand-set mountpoint dir behind) and the result names the location. CLI: mk-nas dataset destroy <ds> [--snapshots]. mk-nas @ 5f0da0e. mk-drive b4068b7: POST /api/nas/datasets/destroy, Locations.rescan also drops vanished discovered locations, trash button on the Datasets page (child datasets only) with snapshots and location named in the typed-confirm prompt. Pool test (needs root+zfs) not run here — CI.
- 2026-09-13 07:14 — dataset.destroy shipped on both sides: agent verb + CLI (mk-nas 5f0da0e), mk-drive route, location drop and Datasets page trash button (mk-drive b4068b7); fixture tests pass, the file-vdev pool test runs in CI — main @ 5f0da0e dataset.destroy: the name typed, refused with children, a share or a copy; snapshots only when asked
