# *Agent events** subscribe to zed (ZFS event daemon) or poll zpool events: a degraded pool is known within seconds, not on the next page load; feeds the health verb and later mk-dashboard

- 2026-09-13 07:30 — Decision: poll, not zed. The agent reads zpool events -v -H every 5 s (MK_NAS_EVENTS_EVERY) by eid inside the agent process — no zedlet, no new privilege path, run.ts as is — and keeps the last 500 in memory (the kernel keeps them since boot; re-read on start; nothing on disk). events verb + health.events (last day, the ones that matter: statechange to a bad state, io/checksum/data/probe_failure/io_failure/delay ereports, removed); repeats fold into one line with a count. scrub_finish/resilver_finish trigger reconcileScans so a scan's job closes within seconds. The exact zpool events -v format (state as "FAULTED" (0x5) or hex, time as two hex words) is from memory — the parser is tolerant, but check it on the box once: mk-nas events --all after a zpool scrub. mk-nas c3f80b1.
- 2026-09-13 07:30 — Events shipped: events.ts poller + events verb + health.events + scan job close-on-finish in mk-nas c3f80b1; /api/nas/events and the 'In the last day' list on the Storage overview in mk-drive c6f3981; 61 agent and 89 drive tests pass. Verify the zpool events -v parse on the real box once. — main @ d55da53 style: prettier over the files touched today
