# System card

- 2026-09-15 05:36 — Agent: power (restart needed from /run/reboot-required + .pkgs; running scrub/resilver, replication, SMART self-test on pool disks read with -n standby), system.reboot / system.shutdown (hostname typed, systemd-run timer 5 s out, second request refused), docs/power.md; contract stays 2. Drive: Overview foot → System section (mk-nas + mk-drive versions, 'A restart is needed' with packages, Reboot / Shut down with typed name, running work and tunnel wording in the dialog), /api/nas/power + /system/reboot|shutdown, audited. Verified in the VM: power output, wrong name refused, reboot from CLI and from the UI (new boot id, timer fired ~5 s after the answer), shutdown powered QEMU off. Not done here: 'a newer release is out' — belongs to Updates from the box. Drive commit 03dc230 — main @ 4699266 Reboot and shut down: power, system.reboot, system.shutdown
