# *Scrub schedule** a per-pool policy (monthly by default) run by the same timer as snapshots; last result kept for the UI

- 2026-09-13 07:17 — Decision: the last scrub result is not written down — ZFS keeps it in the scan line, so scrubDue(interval, pool.scrub, now) in policy.ts is pure and the only state is the interval (scrub_policies table; no row = monthly). Never while a scan runs; at once when never scrubbed or right after a resilver; a canceled scrub waits the interval like a finished one; pools not ONLINE/DEGRADED are skipped so a broken pool does not fail the tick every 15 minutes. Same timer (tick.ts), unit descriptions updated. mk-nas 58cad87.
- 2026-09-13 07:17 — Scrub schedule shipped: scrub.policies/scrub.policy.set + tick in mk-nas 58cad87, CLI scrub policies|policy; Pools page select in mk-drive (NAS mode: the scrub schedule per pool); tests pass both sides — main @ 58cad87 Scrub schedule: a per-pool interval, monthly unless told otherwise, run by the snapshot timer
