---
kanban-plugin: board
updated: 2026-09-17
---

# mk-nas

One card per line, ranked top-down inside each column. `#p0`..`#p3` = priority, `[[name]]` = notes/name.md.

## Now

## Next

- [ ] **Phase 0 — baseline** Ubuntu Server 24.04 on a small PC with Cockpit + 45Drives ZFS and file-sharing plugins; mk-drive on the datasets; two weeks of use; write notes/missing.md with what is still missing #p1 #phase0
- [ ] **Later — apps** docker-compose stacks from a list (mk-drive, Immich), UPS, encryption keys, S3 backup target #p3 #later
- [ ] **mk-dashboard on the NAS** add it to the /opt/mk-drive stack (port 8800, docker socket, /proc /sys /host mounts) once ghcr.io/mkornas/mk-dashboard is pullable without login (it answers 403 today); host vitals, containers, backups and push alerts come for free #p2 #phase5 [[mk-dashboard-on-the-nas]]
- [ ] **Lean install** the stick installs ubuntu-server (758 packages, 2.7 GB, 32 running services, 730 MB RAM idle in the VM) with a 4 GB /swap.img; try the autoinstall source ubuntu-server-minimal and a 1 GB swap file, and have upgrade.sh remove drive images older than the pinned one (the VM held 2.6 GB of them after a day of upgrades) — measure in the VM before the next stick #p2 #install
- [ ] **HTTPS on the local network** so a phone can use the box's own name (nas.home.arpa or a name in your own domain) at home: iOS allows plain http only to IPs and .local names. Options to weigh: a real certificate for a name in a domain you own via Let's Encrypt DNS-01 (Caddy with the DNS provider's token; split DNS points the name at the box), or the drive's port behind Caddy with its internal CA (the phone must trust that root). Off until configured, in the drive's stack like the tunnel; http on 8810 stays for the LAN #p3 #network
- [ ] *CLA for outside contributions** AGPL-3.0 with a commercial license needs the right to relicense contributions: a short CLA (or CLA Assistant) and a CONTRIBUTING.md in mk-nas and mk-drive before accepting the first outside PR #p2 #public
- [ ] *Self-hosted runner on a public repo** vm.yml runs on a self-hosted KVM runner; before registering one, require approval for all fork PR workflows and restrict the runner to the vm workflow (a fork PR can otherwise run code on it) #p2 #ci [[self-hosted-runner-on-a-public-repo-vm-yml-runs-on-a-self-ho]]
- [ ] *Disk spindown** an optional standby timer per pool disk (hdparm -S, kept across reboots), off by default, set on the Disks page; worth it only after 'Idle disks stay idle' #p3 #disks
- [ ] *Disk temperatures over time** load the drivetemp module so SATA temperatures come from hwmon in the 5 s vitals (no smartctl, a sleeping disk is not woken), and show each disk's temperature with a half-hour sparkline on the overview #p3 #disks
- [ ] **Found by name from Windows** Windows Explorer's network view does not list the box: Samba runs without nmbd and there is no WS-Discovery; add wsdd (or wsdd2) to the package so the box shows up under Network, and say in the Shares page which name Windows uses. Found when Déjà Dup on Linux failed on the bare smb://mk-nas name (the pages now show <name>.local) #p3 #shares
- [ ] *Back up the data itself, off the box** replication to another mk-nas exists; add a cloud target (S3, Backblaze B2) with restic or rclone in a transient unit: a schedule, progress as a job, a restore path that is tested, and the credentials handled like the tunnel token (root-only .env, never returned); set up from the drive's Storage section #p2 #backup
- [ ] *Power cuts** with a UPS on USB (NUT), watch the battery and shut the box down cleanly before it dies; show the UPS on the Overview and let the owner set the threshold; nothing installed when there is no UPS #p2 #hardware
- [ ] *Grow the pool** add a disk or a mirror to an existing pool, and keep a hot spare, from the Pools page instead of the shell: the plan shown before it runs (what ZFS will do, what cannot be undone), the typed pool name for anything destructive #p2 #pools

## Later

## Done
- [x] *Small things from the audit** network revert() removes the pending file before netplan apply (a failed revert is forgotten); apply() in shares.ts writes through one fixed temp name (two share changes at once race); the socket's 64 KB limit counts the whole buffer, not a line; update downloads have no size cap; finish reading cli.ts, zfs.ts, alerts.ts, events.ts, system.ts #p3 #security [[small-things-from-the-audit-network-revert-removes-the-pendi]]
- [x] *Run the stick's lxd mask in the VM** autoinstall now runs systemctl mask lxd-installer.socket in the target; it parsed but never ran: make vm, then check the socket is masked and the first user cannot trigger an LXD install #p2 #security [[run-the-stick-s-lxd-mask-in-the-vm-autoinstall-now-runs-syst]]
- [x] **Release 0.9.1** the audit's fixes in both repos as a signed pair: mk-drive 0.9.1 pinned, docs/upgrading.md says what changes on a box #p1 [[release-0-9-1]]
- [x] **Security: what a fresh box leaves open** the first user is in the lxd group and lxd-installer lets that group install LXD without sudo (root without a password); rpcbind listens on 111 with no NFS share: the stick masks lxd-installer.socket, a first install leaves rpcbind off until the first NFS share, docs/first-install.md has the commands for boxes installed earlier #p2 #security [[security-what-a-fresh-box-leaves-open]]
- [x] **Security and stability audit after going public** the agent, the installer, CI and the update path read end to end; six fixes with tests: an unwritable audit log no longer takes the agent down, share.set refuses smb.conf's own section names and duplicate share names, a second location of one name is refused, a snapshot is pruned only once its replacement exists, each copy prunes only its own repl-<id>-* snapshots, backup.restore only from a root-only dataset #p1 #security [[security-and-stability-audit-after-going-public]]
- [x] *Tell me when something is wrong** the box knows about a degraded pool, a failing or overheating disk, a failed scrub, a failed settings backup and a waiting update, but nothing reaches the owner unless they open the page: a small alert engine in the agent (one event per condition, remembered so it does not repeat, cleared when the condition goes) and delivery the drive can use — phone push through mk-drive first, email as an option; quiet by default, no chatter #p1 #alerts [[tell-me-when-something-is-wrong-the-box-knows-about-a-degrad]]
- [x] **Pilot box to 0.6.0** done long ago (box on 0.8.0) [[pilot-box-to-0-6-0]]
- [x] *Security: CI and packaging hardening** persist-credentials false, actions pinned by SHA, permissions on test.yml, vm.yml dispatch only from main and an isolated runner, chmod -R go-w in deb.sh, gpgv the Ubuntu SHA256SUMS, cloudflared by digest, cap_drop/no-new-privileges, re-validate DB rows in replicate, upgrade.sh UNSIGNED hint removed #p2 #security [[security-ci-and-packaging-hardening-persist-credentials-fals]]
- [x] *Security: NFS open to every private network** the default exports rw to 10/8, 172.16/12, 192.168/16 with no auth: no default clients, ask for them #p2 #security [[security-nfs-open-to-every-private-network-the-default-expor]]
- [x] *Security: installer defaults** postinst adds the first user to docker (root without a password) on every upgrade; the stick allows ssh password login: drop the docker group, keys-only or document #p2 #security [[security-installer-defaults-postinst-adds-the-first-user-to]]
- [x] *Security: first boot on the LAN** port 8810 on all interfaces before an admin exists: a one-time setup token printed on the console / by mk-nas #p1 #security [[security-first-boot-on-the-lan-port-8810-on-all-interfaces-b]]
- [x] *Security: drive image pulled by tag, unchecked** compose pulls ghcr.io/mkornas/mk-drive:X.Y.Z when missing (hand installs, pruned images, iso.sh docker pull): pull_policy never, verify the image id from the signed release before up, iso.sh uses the verified tgz #p1 #security [[security-drive-image-pulled-by-tag-unchecked-compose-pulls-g]]
- [x] *Security: signing is blind to what CI built** sign-release.sh signs whatever SHA256SUMS the release has; the drive image hash comes from mk-drive's unsigned SHA256SUMS: commit the drive tgz hash with the pin, rebuild or compare the deb locally, write release.json and SHA256SUMS in the signing step #p1 #security [[security-signing-is-blind-to-what-ci-built-sign-release-sh-s]]
- [x] **Disabling or deleting a drive account leaves SMB access** Samba does not know the drive disabled someone: on disable, take the account off every share list (and restore nothing on enable); on delete, remove its SMB user (user.remove), which also drops it from the lists #p1 #shares #security [[disabling-or-deleting-a-drive-account-leaves-smb-access]]
- [x] **SMB access per share** each SMB share lists who may open it (read or read and write), prefilled in the drive from the grants on that location; Samba gets exactly that list (valid users, read only, write list); a share nobody may open is left out of smb.conf; existing shares become admins-only when the drive first sees them; removing an SMB user drops them from every list #p1 #shares #security [[smb-access-per-share]]
- [x] *Security: user verbs touch any Unix account** user.remove runs userdel on any existing account (system users, the admin login); user.set/smbPassword modify existing accounts: only accounts mk-nas recorded #p0 #security [[security-user-verbs-touch-any-unix-account-user-remove-runs]]
- [x] *Security: restore to root code** backup.restore takes mk-drive.env from any dataset into /opt/mk-drive/.env, which mk-nasd loads (EnvironmentFile: NODE_OPTIONS runs code as root); restore copies follow symlinks in the container-writable /opt/mk-drive/data: no EnvironmentFile (read DRIVE_UID/GID only), allow-listed .env keys, no symlinks. Audit 2026-09-15 #p0 #security [[security-restore-to-root-code-backup-restore-takes-mk-drive]]
- [x] **Cloudflare Tunnel from the UI** Storage → Network gets a 'Reach the drive from outside' card: paste the tunnel token, see the hostname it serves, whether it is connected and when it last was. cloudflared runs as a second container in the /opt/mk-drive stack, started only when a token is set (the agent never installs packages from a third-party repo); an agent verb stores the token in a root-only file, the drive only ever shows it masked, the settings backup includes it. Setting, changing or removing the tunnel is refused through the tunnel itself (it would cut the connection in use) — from home only. Hostnames stay managed in Cloudflare's dashboard. Until then: ssh, docs/first-install.md 'Reach the drive from outside' #p2 #remote [[cloudflare-tunnel-from-the-ui]]
- [x] *Idle disks stay idle** hourly snapshots are taken even when nothing changed, which writes to the pool every hour; skip a scheduled snapshot when the dataset's written property is 0 since the last one. SMART reads from the drive (disks, health, anything polling health like mk-dashboard) run smartctl without -n standby and would wake a sleeping disk: read standby-aware and show the last reading with 'asleep' #p2 #disks [[idle-disks-stay-idle-hourly-snapshots-are-taken-even-when-no]]
- [x] *Self-tests at night, and shown** the tick starts a long SMART self-test as soon as one is due, at any hour (the pilot box's two data disks started theirs at 08:01 and 08:56, hours of full-surface reads and a few degrees warmer); start them only in a night window, one disk at a time as now, and show a running self-test on the overview's disk bay so a warm idle disk is explained #p2 #smart [[self-tests-at-night-and-shown-the-tick-starts-a-long-smart-s]]
- [x] **Self-tests on disks that cannot run them** the tick picks an NVMe OS disk for its monthly long self-test, smartctl -t long exits 4 (no self-test support), and every tick after that counts as failed (mk-nas-snapshot.service shows failed every 15 min; snapshots and scrubs still run). Skip a disk whose smartctl -c reports no self-test support (ATA capabilities / NVMe optional admin commands) and do not fail the tick on it. Found on the pilot box after the 0.6.0 upgrade #p1 #bug #smart [[self-tests-on-disks-that-cannot-run-them]]
- [x] **Updates from the box** the agent checks GitHub Releases daily, the Storage pages show the new version with its notes and an Install button that runs the same steps as install/upgrade.sh (settings backup, package, pinned drive) as a detached job; verified against a signing key on the box, never from a branch; Ubuntu's reboot-required shown too — docs/releasing.md 'Later' #p2 #updates [[updates-from-the-box]]
- [x] **Go public** mk-nas and mk-drive as public repos: current files cleaned of the author's network, hardware and accounts; AGPL-3.0; full history archived to private *-archive repos, main squashed to one commit, old tags and releases dropped, GHCR image public, first public release. Blocks Updates from the box (a public release to check) #p1 #public [[go-public]]
- [x] **System card** a System section on the Storage pages: the agent's and the drive's versions and whether a newer release is out (with 'Updates from the box'); 'a restart is needed' when Ubuntu's own updates brought a new kernel (/var/run/reboot-required and its package list); Reboot and Shut down, confirmed by typing the box's name, the agent answering first and acting a few seconds later; before confirming, a warning listing running work (scrub, rebuild, replication send, SMART long test — they resume, but the person should know); a shutdown requested through the Cloudflare Tunnel says plainly that the box stays off until someone presses the power button. New agent verbs system.reboot and system.shutdown, audited #p2 #system [[system-card]]
- [x] **Shares page: say when nobody can connect** a share lets in only the mk-nas-smb group, which exists once someone sets an SMB password on the Account page; until then the Shares page (and a dataset's share dialog) says 'Nobody can connect over SMB yet — set an SMB password under Settings → Account → Network access', and lists who can (users with a password), so a Linux login like the installer's user is not mistaken for an SMB account. Found on a first install #p2 #shares [[shares-page-say-when-nobody-can-connect]]
- [x] **snapshot.rollback cannot succeed** it takes a before-rollback snapshot first, which is newer than the target, so zfs rollback (no -r, by design) always refuses; nothing changes but a stray snapshot is left, and the UI promises a saved copy. Found by the real-pool test in the VM (the fake-runner test hid it). Decide: (A) honest rollback — refuse while newer snapshots exist, no safety snapshot, the UI says changes since then are lost, per-file versions stay the safe path; (B) clone-and-swap that keeps the current state as <ds>-before-rollback-<stamp> (renames, local properties, children and replication to handle); (C) drop dataset rollback from the UI, restore files from .zfs/snapshot and versions. Blocks a green release (the pool test) #p1 #bug [[snapshot-rollback-cannot-succeed]]
- [x] **Config backup** the agent's database, Samba's passdb, the ssh key and 90-mk-nas.yaml exported daily into a dataset that replicates (tank/mk-nas-config), and a restore verb with the typed name — a dead OS disk becomes stick + pool.import + one restore (from notes/ideas.md) #p2 #survive [[config-backup]]
- [x] **Replication target mountpoint** a received dataset keeps the source's mountpoint property (seen in the VM: tank/backup says /srv/locations/photos, the same as tank/photos); receive with -u and set canmount=off or mountpoint=none on the target so a copy never mounts over the original #p2 #phase4 [[replication-target-mountpoint]]
- [x] *VM smoke test in CI** a nightly job that builds the VM ISO and runs the unattended install in QEMU with KVM on a self-hosted runner, then checks /api/meta and one mk-nas call #p3 #ci [[vm-smoke-test-in-ci-a-nightly-job-that-builds-the-vm-iso-and]]
- [x] *mk-nas CLI: shell completion** bash and zsh completion for commands, pool and dataset names (from the socket) #p3 #cli [[mk-nas-cli-shell-completion-bash-and-zsh-completion-for-comm]]
- [x] *Audit rotation** /var/log/mk-nas/audit.jsonl under logrotate, weekly, keep 12 #p3 #install [[audit-rotation-var-log-mk-nas-audit-jsonl-under-logrotate-we]]
- [x] *Location ownership** a dataset made as a location is owned by uid 1000; make the uid/gid configurable in install.sh (.env) for a container that runs as someone else #p3 #install [[location-ownership-a-dataset-made-as-a-location-is-owned-by]]
- [x] *SMART self-tests** smart.test short|long with the result in the smart verb; a monthly long test per disk on the timer #p3 #phase5 [[smart-self-tests-smart-test-short-long-with-the-result-in-th]]
- [x] *Network verbs** network (interfaces, addresses, gateway, DNS, link speed, DHCP or static from netplan) and network.set (hostname via hostnamectl, one netplan file /etc/netplan/90-mk-nas.yaml, applied with a 120 s revert unless the UI confirms it can still reach the box — the safe way to change an address remotely); mDNS via avahi so <hostname>.local works out of the box #p2 #phase5 [[network-verbs-network-interfaces-addresses-gateway-dns-link]]
- [x] *System vitals in the agent** a system verb: uptime, load, CPU %, memory, swap, per-NIC rates, disk I/O per pool member, hwmon temperatures, sampled every 5 s with a 30-minute ring buffer in memory (nothing on disk); mk-dashboard stays the deep monitor, this is the glance #p2 #phase5 [[system-vitals-in-the-agent-a-system-verb-uptime-load-cpu-mem]]
- [x] *Agent events** subscribe to zed (ZFS event daemon) or poll zpool events: a degraded pool is known within seconds, not on the next page load; feeds the health verb and later mk-dashboard #p2 #phase5 [[agent-events-subscribe-to-zed-zfs-event-daemon-or-poll-zpool]]
- [x] *jobs verb for real** scrub and resilver as jobs with progress and history in SQLite; the UI polls jobs instead of parsing scan lines #p2 #phase5 [[jobs-verb-for-real-scrub-and-resilver-as-jobs-with-progress]]
- [x] *Scrub schedule** a per-pool policy (monthly by default) run by the same timer as snapshots; last result kept for the UI #p2 #phase2 [[scrub-schedule-a-per-pool-policy-monthly-by-default-run-by-t]]
- [x] *dataset.destroy** typed name; refused with children; snapshots go with it only when the caller says so; a location dataset also leaves the drive's locations #p2 #phase2 [[dataset-destroy-typed-name-refused-with-children-snapshots-g]]
- [x] **Decide the base and the boundary** confirm Ubuntu LTS + OpenZFS only, and the root agent behind a Unix socket with allow-listed argv verbs; write docs/base-os.md and docs/agent-boundary.md (docs/ui-lives-in-mk-drive.md is done) #p1 #decision [[decide-the-base-and-the-boundary]]
- [x] **mk-nas CLI** agent/src/cli.ts installed as /usr/local/bin/mk-nas: the same verbs over /run/mk-nas.sock (root or group mk-nas), same audit; mk-nas pools | pool tank | disks | health | dataset create tank/x --quota 10G --location | snapshot tank/x | policy tank/x 24 7 4 3 | call <verb> <json>; for ssh, scripts, and the day the UI is down #p2 #phase2 [[mk-nas-cli]]
- [x] **NAS alerts in mk-dashboard** DASH_NAS_SOCKET: the dashboard mounts /run/mk-nas.sock (group mk-nas) and polls the health verb — degraded pool, failing disk, pool over 90% become alerts in its existing pipeline (push, Telegram, webhook) and a Storage tile; read-only, one verb; the code lives in the mk-dashboard repo #p2 #phase5 [[nas-alerts-in-mk-dashboard]]
- [x] **Phase 6 — keep** the agent as a .deb with a version the drive checks, upgrade notes #p3 #phase6 [[phase-6-keep]]
- [x] **Phase 5 — survive** degraded pool view, disk.replace + resilver progress, pool.import of a foreign pool, alerts through mk-dashboard #p2 #phase5 [[phase-5-survive]]
- [x] **Phase 4 — copy** replication jobs: zfs send -I over ssh to another host, resumable, on a timer, last result and history in the UI #p2 #phase4 [[phase-4-copy]]
- [x] **Answer the open questions** SATA ports and an OS SSD in a small PC; mirror vs raidz1 default; an existing ZFS host or another mk-nas as the replication target — PLAN.md §6 #p1 #decision [[answer-the-open-questions]]
- [x] **Phase 3 — share** SMB + NFS shares generated from the agent's db with reload, SMB password on the mk-drive account page, Time Machine flag #p2 #phase3 [[phase-3-share]]
- [x] **Phase 2 — make** pool.create (mirror, raidz1/2, by-id, typed name), dataset.create with quota/compression/atime, snapshot now, per-dataset snapshot policy on a systemd timer; a new dataset offers itself as a mk-drive location #p2 #phase2 [[phase-2-make]]
- [x] *The stick carries the mk-drive image** make iso saves ghcr.io/mkornas/mk-drive (or DRIVE_IMAGE) with docker save into /mk-nas/mk-drive-image.tgz; install.sh copies it, mk-drive.service loads it before the first compose up; the image stays private on ghcr and updates need docker login on the NAS #p1 #install [[the-stick-carries-the-mk-drive-image-make-iso-saves-ghcr-io]]
- [x] **Phase 1b — the stick** make iso: Ubuntu autoinstall remastered with xorriso; asks hostname, admin email, OS disk; installs the agent, Docker and the mk-drive stack; reinstalling the box becomes a coffee break #p1 #phase1 [[phase-1b-the-stick]]
- [x] **Phase 1 — see** mk-nasd read-only verbs (disks by-id + SMART, pools, datasets, snapshots, scrubs, health) over the socket, audited, tests on a loop-device pool; mk-drive: DRIVE_NAS_SOCKET, /api/nas proxy (admins), Storage → Disks, Pools, Datasets, Snapshots, Health #p1 #phase1 [[phase-1-see]]
